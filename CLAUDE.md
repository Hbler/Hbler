# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This Repository Is

This is Hugo Bler's GitHub profile repository (`Hbler/Hbler`). The sole file is `README.md`, which GitHub automatically renders as the public profile page at github.com/Hbler. There is no build process, no test suite, and no application code.

## Working With This Repo

All changes are edits to `README.md`. The file uses GitHub-flavored Markdown with embedded HTML for badge/image layouts. Previewing changes requires either the GitHub web editor or a local Markdown renderer that supports HTML — standard Markdown previewers may not render the badge images correctly.

The README is bilingual (English + Brazilian Portuguese), with Portuguese translations in `<sup>` tags below each English line. Maintain this pattern when adding content.
